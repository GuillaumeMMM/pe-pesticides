# Pesticides world map data visualization

## Run the project

```bash
# install dependencies
yarn install

# run the dev live preview
yarn run dev
```

## Create a static build

```bash
# the built project will be in /build
yarn run build

# preview the static build locally
cd /build
npx http-server

```
